{
  "name": "costrack-mcp",
  "version": "1.0.0",
  "description": "Agent Cost Control Layer — MCP server for LLM cost tracking, budget enforcement, and spend analysis",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.0.0"
  }
}
